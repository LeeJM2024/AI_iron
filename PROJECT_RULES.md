# 项目书评分约束（长期维护基准）

本文件固化《煤气发电量预测与发电优化》项目书中对算法和提交的约束，后续模型选择、自测和提交检查必须遵守。

## 初赛评分口径

- 数据一致性与预处理质量：50%。
- 短周期预测（2 小时、8 个 15 分钟步长）：50%。
- 预测指标为 `1-MAPE`；滚动起点和缺失未来标签按官方评分程序处理。
- 复赛/半决赛再加入长周期（24 小时、96 步），短长周期各占 50%。

## 硬性约束

- 严禁使用参考时刻之后的数据、测试标签或人工逐条修正测试结果。
- 多表统一 15 分钟时间粒度，时间索引连续、去重、可复现。
- 工程特征列统一使用 `feat_` 前缀；原始字段保持题目命名。
- 输出预测不得缺行、重复、错位或包含非有限值。
- 发电计划必须优先保障用户供气和气柜安全区间，分别满足煤气守恒、机组容量、在线机组和爬坡约束。
- 优化煤气消耗字段统一使用 `opt_` 前缀。
- 提交代码不得依赖联网模型服务或商业闭源在线接口。

## 本地自测边界

`self_score.py` 是项目书口径的本地近似评分器，主要验证提交格式、输入字段完整性和初赛短周期 `1-MAPE`。它不是官方排行榜评分器，不能保证官方归一化分数或名次。模型选型必须优先看未参与选权的历史滚动窗口，禁止根据官方测试标签反复调参。
