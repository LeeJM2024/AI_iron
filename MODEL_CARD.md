# TEST_LABEL_CALIBRATED_AFFINE

发布日期：2026-09-16。

本版本使用公开测试标签监督学习四组全局参数：每个目标分别在 15–30 分钟和 45–120 分钟
使用一组非负组合权重及偏差。权重总和限定为 0.9–1.1，偏差限定为拟合标签中位数的 ±10%。
从既有算法预测组合计算输出，并限制功率范围及 generator_1 不超过 generator_all。

标签进入的是参数拟合与模式选择。参数重放本身不查找真值，但这**不等于整个方法无标签泄漏**。
本地训练数据已经包含评测标签，不能据此声称突破正常预测算法的数学上限或获得独立盲测性能。

选择校准模式的六小时分块诊断剔除了验证目标时刻及两小时预测扩展区间，
但训练可能发生在验证块之后。模型家族、诊断和模式选择也已经多次观察这段数据，
因此分块结果仍属于回顾性测试期研究，而非严格时序外推验证。

本地平均 MAPE 为 0.04402147166692736；代入用户提供的
`total = 50 + 50*(mean_1MAPE-0.688)/0.312`，在质量为 50 的假设下总分约 92.9453。
根据旧 V30 文档的经验差值估算平台约 93.06；用户 2026-09-16 回传的平台实测为 93.03（与估算差 0.03）。实测值记录在 SUBMISSION_RUNBOOK.md 第十一节；release 的 manifest.json 保持提交时的冻结状态（official_score=null，由 tests/test_current_release.py 断言保护）。

仅在用户获授权的答案可见评测/研究模式下使用这一版本。用户转述了该阶段允许使用公开标签，
仓库没有独立核实主办方授权；不要把这里的说明误认为主办方认证。

原始标签不随本发布打包。预测库保存的是冻结算法输出。
数据源哈希、文件哈希和具体权重见 release 目录的 REPORT.json、manifest.json 和 parameters.json。
固定预测库重放通过容差 1e-8，发布 ZIP 校验和固定，常规运行不会改动冻结文件。
