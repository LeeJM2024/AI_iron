# 同输入条件下的滚动更新与相对误差模型

日期：2026-09-13。用户要求继续提高预测准确度。
第五版已获平台总分 92.6、质量 50/50；本版尚未提交，官方得分未知。

本轮候选：`output/adaptive_v10/LeeJM_gas_predict_prelim.zip`。
内部模型名为 `online_v10`，包含相对误差模型和少量周期更新模型。

## 结果

| 本地八步等权 MAPE | 已验证第五版 | 新候选 |
| --- | ---: | ---: |
| generator_1 | 5.4158% | 5.3652% |
| generator_all | 3.9779% | 3.9670% |

两个目标都是小幅改善，没有达到足以保证进入前十的程度。
这不是平台 acc 分数，平台评分口径与本地原始标签口径存在细小差异，
不能将这些数值直接换算为新的官方总分。

输入 `input.csv` 与质量满分第五版**逐字节一致**：55 个数值列、192 行、
无缺失/重复/常数列，z-score 尾部计数为 0。未通过增加提交字段来获取预测收益。
这一点降低了输入质量回退风险，但没有代替新的官方评分。

## 本次方法及选择

1. 周期更新 Ridge：分别比较 7/21/60 天窗口、每 6 小时更新，以及 21 天窗口每 2 小时更新。
   预处理、窗口、正则化、更新频率和集成权重由 4 月开发数据确定。
   每次更新只允许使用严格早于更新起点、当时已经到达的观测。
   对第 h 步，训练特征还需前移 h 个步长，保证其未来标签已知。
2. 相对误差线性模型：将目标改为 `future/current - 1`，对负荷历史作相对归一化。
   平方损失的权重为 `(current/future)^2`；稳健版本使用 IRLS 近似加权 MAE，
   结合训练窗口内的时间衰减。IRLS 为平滑近似，不声称精确求解 MAPE 最优。
3. 原第五版 Ridge、LightGBM、共享步长模型仍在候选池中。
   在线更新单独加入并无稳定收益，加入相对误差模型后组合才通过准入。

准入标准未临时降低：对每个目标，四个开发窗口的留一窗口平均 MAPE
需要至少改善 0.5%。没有根据本轮 5 月分数重新调节超参数、门槛或权重。

| 留一开发窗口 MAPE | 第五版 | 在线更新候选池 | 在线更新＋相对误差候选池 |
| --- | ---: | ---: | ---: |
| generator_1 | 5.5402% | 5.5426% | 5.5088% |
| generator_all | 4.1309% | 4.1389% | 4.0807% |

最终组合相对第五版的开发误差改善约 0.57%/1.21%。
4 月 29 日诊断窗口未拟合集成权重：单组 MAPE 6.0361% → 6.0857%，
总负荷 6.8552% → 6.6624%。单组在此窗口退步，不能声称所有窗口都改善。
该窗口在此前研究中已查看，不是整个研究从未接触过的盲测。

## 重要部署协议变化

本版有少量模型系数会在部署期间更新，**不再是所有模型参数整段冻结**。
不能把“未使用 5 月标签调超参数”说成“从不使用 5 月观测拟合系数”。
已到达的过去负荷既可作为输入，也被在线成员用于后续更新。
任何更新都不得读取更新时刻及之后的标签；每个预测只能使用截至自身起点的信息。

这建立在赛事允许逐起点获取新观测的滚动协议之上。
如果官方另有要求禁止部署期更新参数，则不能提交本在线版本，需要改用冻结模型。

最终更新审计共 512 条逐成员/目标/步长记录。
每条记录均验证 `last_label < update_origin`。
最后的更新标签时刻分别是 `online21_2h` 的 2025-05-02 21:45，
和 `online7_6h` 的 2025-05-02 17:45，而不是读取整个测试集后回头生成预测。

## 验证与复现

- 相关 16 项测试全部通过，包括原冻结流程和新增在线/相对误差流程。
- 修改任意未来观测，不改变其之前的预测。
- 模型序列化、从实际 CSV 输入重放、首/中/末历史截断检查均通过。
- 在线重放缺少必要的历史输入时显式报错，不补造历史或静默回退。
- ZIP 根目录只有 `input.csv`、`s_result.csv`，CRC 检查通过。
- 第五版 92.6 分提交包哈希不变，原始数据未改动。

初始训练准备约 9.3 秒，包含周期重训的全量推理约 15.4 秒，
完整序列化/截断/CSV 重放、审计、打包及测试约 86.7 秒。
本次复用第五版中参数和训练数据一致的 LightGBM/共享步长成员，
因此初始训练时间不是所有基础模型从零训练时间。

Windows：`./run_adaptive.ps1` 从冻结选择重训并导出。
`./run_adaptive.ps1 -Develop` 重做基础模型、在线模型、相对误差模型及选择实验。
选择文件：`artifacts/adaptive_selected/selection.json`。
审计：输出目录下的 `online_update_audit.csv`、`training_audit.csv`、
`quality_audit.json`、`run_metadata.json`。

当前仍为本机 Python 3.13 环境，尚未另行验证官方 Python 3.10。
整个项目已经反复查看 5 月指标与平台反馈，不能把本轮最终误差当作独立泛化估计。

ZIP SHA256：
`1449bf849c0a6c92c3a3b631f02c3935e00efcf2359feb495db0b5f721426dde`

本轮没有自动提交比赛平台、没有推送 GitHub，也没有承诺官方涨分或前十名次。
