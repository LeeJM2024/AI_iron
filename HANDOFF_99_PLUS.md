# 煤气发电预测项目：99+ 提分与接班文档

更新时间：2026-09-12

本文件用于把当前任务完整交给下一位工程师。请先读 `PROJECT_RULES.md` 和原始项目书，再开始实验。当前任务的目标是提高排行榜成绩，目标为 99+；所有改进必须遵守时序因果约束，禁止利用 5 月测试标签选模型或人工修正测试行。

## 1. 官方反馈与真实基线

最近一次平台提交反馈：

```text
split=gas_power
horizons=[15, 30, 45, 60, 75, 90, 105, 120]
quality=40.0/50
(miss:10.0, dup:5.0, out:0.0, intv:5.0,
 invalid_col:0.0, feat:5.0, comp:15.0)
acc=41.7/50
1mape_1=0.9438
1mape_all=0.9526
total=81.7
```

这比任何本地模拟分更权威。它说明：

1. 时间覆盖、重复、缺失、特征前缀和字段完整性基本正确。
2. `out=0` 是最明确的质量损失，异常处理项损失 10 分。
3. 平台 `acc=41.7/50` 不是简单的 `平均(1-MAPE) * 50`。平台存在未公开的标准化、基线折算或附加规则，本地评分器目前不能精确复刻该映射。
4. 若质量项恢复到 50，而预测不变，按加法估计总分约为 91.7。达到 99+ 仍需把平台预测分从 41.7 提高到至少 49。

当前根目录预测文件来自较新的模型，本地原始标签复算为：

| 目标 | 8 步 MAPE | 1-MAPE |
| --- | ---: | ---: |
| `generator_1` | 5.4268% | 94.5732% |
| `generator_all` | 4.1020% | 95.8980% |

较最近平台提交中的 94.38% / 95.26% 略好，但没有新的平台提交结果证明平台分数提升。

## 2. 项目书固定约束

初赛评分是数据校验与预处理 50%，短周期预测 50%。短周期为 8 个 15 分钟步长，即 15 至 120 分钟。复赛和半决赛才按短周期 50%、长周期 50% 评价。

必须始终满足：

- 参考时刻之后的数据不得参与该起点的特征、校准和预测。
- 测试标签只能用于最终独立报告，不能用于模型、权重、超参数或规则选择。
- 多表按 15 分钟对齐；输出不缺行、不重复、不错位。
- 原始字段名称保持官方名称；工程字段必须以 `feat_` 开头。
- 优化字段必须以 `opt_` 开头。
- 不得人工逐条修改测试预测。
- 所有选型使用训练数据内部的滚动时序验证。

完整约束见 `PROJECT_RULES.md`。

## 3. 当前代码和产物状态

主要代码：

- `forecasting.py`：因果特征、LightGBM/CatBoost 残差模型、共享步长模型、精确凸权重。
- `experiment.py`：训练集内部滚动选权。
- `run_official.py`：正式训练、预测、输出和调度。
- `validate_outputs.py`：独立复算预测与调度约束。
- `self_score.py`：项目书口径的本地近似评分器。
- `tests/test_invariants.py`：泄漏、序列化、电价和 MILP 约束测试。
- `tests/smoke_test.py`：脏数据端到端测试。

已完成的代码修改：

1. `self_score.py` 已按四张官方表动态识别原始字段，不再把合法字段误判为非法特征。
2. `self_score.py` 已允许合法的零负荷预测，并修复质量分重复计分。
3. `tests/smoke_test.py` 已修复直接运行时找不到项目模块的问题。
4. `run_official.py` 新增 `_quality_input_frame`：用训练截止点之前的 Tukey/IQR 阈值盖帽原始输入，并输出 29 个 `feat_*_outlier` 标记。
5. 根目录、`output/official/` 和 `output/official_latest/` 的 `input.csv` 已重新生成，共 337 列；本地异常阈值检查为 0 个违规值、29 个异常标记列。

当前 Git 工作区不是干净提交状态：

```text
M input.csv
M run_official.py
M tests/smoke_test.py
?? PROJECT_RULES.md
?? self_score.py
```

不要撤销这些改动。它们属于本轮明确的质量评分修复。

### 当前阻塞问题

`validate_outputs.py --output-dir output/official_latest` 当前失败：

```text
TypeError: 'LGBMRegressor' object is not iterable
```

原因是保存的 `forecast_model.joblib` 与当前 `forecasting.py` 的 seed-bag 模型结构不一致。旧模型内部保存单个 estimator，当前推理代码预期 estimator 列表。

接手后必须二选一：

1. 推荐：使用当前代码和冻结选择重新完整运行 `main.py --official`，生成同版本模型与全部产物；
2. 兼容方案：让 `_learned_predictions` 同时接受单 estimator 和 estimator 列表，并增加旧模型加载测试。

在 `validate_outputs.py` 完整通过前，不得把当前目录打包为最终版本。

### 不完整实验

`artifacts/short_30d_120/` 是被用户要求停止时中断的实验，只有 `data_profile.json`，不能用于结论。

`artifacts/short_trees320/` 已完整运行。320 棵树在 4 月 29 日保留窗口上退化：

| 目标 | 320 树短周期 MAPE |
| --- | ---: |
| `generator_1` | 5.8371% |
| `generator_all` | 6.8961% |

结论：320 树候选淘汰，不要直接加入正式选择。

在线残差校准也已测试。32/64 点窗口在 4 月 27 日改善，但在 4 月 29 日恶化，跨窗口不稳定，暂不接入。

## 4. 99+ 的正确工作路径

### 阶段 A：先拿回确定的质量分

目标：平台 `quality` 从 40/50 提到 50/50。

1. 完整重跑正式流程，使新异常盖帽逻辑真正生成提交文件。
2. 本地检查所有 29 个原始字段均在训练期 Tukey fence 内。
3. 确保每个原始字段都有对应 `feat_<field>_outlier`。
4. 检查 `input.csv` 无 NaN/Inf、192 行、时间唯一、15 分钟连续、所有非原始工程列以 `feat_` 开头。
5. 做一次受控平台提交，只改变 `input.csv` 的异常处理，保持 `s_result.csv` 不变。这样可以确认 `out` 是否从 0 变为 10，而不会把质量变化误认为模型变化。

验收门槛：平台反馈 `quality=50.0/50`。若仍为 40，必须取得平台对 `out` 的具体检测定义，再调整盖帽规则；不要盲猜多次提交。

### 阶段 B：校准官方预测分映射

平台的 `acc=41.7` 与两个公开 `1-MAPE` 无法按简单平均复现。要达到 99，必须先知道平台怎样把预测指标转换为 50 分。

建议使用最多两次受控提交：

1. 提交 A：新 `input.csv` + 当前 `s_result.csv`，记录 quality、acc 和两个 1-MAPE。
2. 提交 B：保持 `input.csv` 完全相同，只替换一个经过训练内验证的新预测版本。

由 A/B 的差值判断：

- `acc` 是否线性依赖两个 1-MAPE；
- 两个目标权重是否相同；
- 是否存在官方基线扣分或归一化；
- 文件质量是否会连带影响 acc。

每日最多提交 5 次。每次必须只有一个可解释变量变化，并在实验日志中记录文件 SHA256、配置、平台反馈和结论。

### 阶段 C：重建可信短周期验证

当前训练数据在 4 月 18 日附近存在明显工况切换，单一两天验证窗口方差很大。新的验证协议应做到：

1. 训练与验证严格按时间切分，标签结束时间不超过 cutoff。
2. 工况切换后的 4 月 19 至 30 日按天建立至少 8 个短周期验证起点。
3. 每个验证块建议 24 小时，块间可重叠但最终统计按目标、步长、日期分层。
4. 另保留一个完全不参与超参数选择的最终窗口，例如 4 月 29 日。
5. 报告：总体 cell-level MAPE、逐 horizon MAPE、逐日均值/标准差、最差日和 P90。
6. 新模型只有在多数日期改善、总体改善且最差日不显著退化时才能进入正式集成。

不要只看一个 48 小时窗口，也不要再用 5 月 1 至 2 日选择配置。

### 阶段 D：短周期模型优先级

按预期收益排序，建议依次实验。

#### D1. 工况条件化的残差模型

现模型对 h<=48 使用 90 天训练窗口，包含大量旧工况数据。虽然预测残差比预测绝对值更抗漂移，但煤气效率和控制策略也可能变化。

实验矩阵：

- 训练窗口：14、21、30、45、60、90 天；
- 时间衰减半衰期：无、7、14、21、30 天；
- `generator_1` 与 `generator_all` 分别选择，不要求共享窗口；
- 树数：80、120、180；叶子数：7、15、31；
- 所有组合先用 h=1、2、4、8 粗筛，再对胜者运行完整 h=1..8。

验收：相对当前配置，保留窗口两目标平均 MAPE 至少下降 0.20 个百分点，且 8 个滚动日中至少 6 日不退化。

#### D2. 注入未来已知电价

电价表是预测时已知的外生变量，不属于泄漏。加入：

- 起点电价 `price(t)`；
- 目标电价 `price(t+h)`；
- 价差；
- 当前/目标价格档位 one-hot；
- 距下一次价格切换的分钟数；
- 目标价格档位剩余分钟数；
- 是否跨越价格边界。

`generator_all` 在 post-regime 数据中对跨价差的相关性随 horizon 增大，在 h=8 约为 0.154，值得正式验证。电价特征必须由 `price.xlsx` 和时间戳生成，不得读取未来观测值。

#### D3. 煤气侧先行特征

post-regime 的初步检查显示：

- `generator_1` 下一步变化与 `air_heater_4` 当前值的相关系数绝对值约 0.227；
- `generator_all` 下一步变化与 2 号气柜当前值的相关系数约 0.269；
- `generator_all` 与 `air_heater_4` 约 0.166。

需要加入更有针对性的特征，而不是只堆通用 rolling mean：

- 气柜 1/2/4/8 步斜率、加速度、距安全边界比例；
- 各热风炉和用户的 1/2/4 步变化；
- 发电耗气量和机组负荷的动态效率；
- 三类发电耗气量的份额和变化；
- 产气、用户耗气、发电耗气和柜位变化的同气种守恒残差；
- 这些量在 h=1..8 的 horizon interaction。

先用训练期 permutation importance 或 SHAP 验证稳定性。只保留跨日稳定特征。

#### D4. 分解总负荷

建立：

```text
other = generator_all - generator_1
generator_all_pred = generator_1_pred + other_pred
```

分别预测摆动较大的 `generator_1` 和更有计划性的 `other`。最终加入直接预测 `generator_all` 与分解预测两个候选，再由折外预测学习融合权重。不要用未经验证的硬替换。

#### D5. 局部相似工况模型

对每个起点从历史中寻找相似状态：当前负荷、最近两小时轨迹、气柜趋势、发电耗气结构、时刻、电价档位。用相似日未来 8 步的增量轨迹作为候选 member。

必须使用训练期标准化尺度，并限制候选样本标签完全位于 cutoff 前。建议从 k=5、10、20、40 开始，使用距离加权中位数，避免单个异常日主导。

#### D6. 多输出序列模型

在树模型提升趋于饱和后，再测试轻量 TCN/N-HiTS/TFT。训练数据只有约 1.1 万行，深度模型很容易过拟合，因此必须满足：

- 输入窗口 8/16/32 步；
- 同时输出两个目标的 8 个 horizon；
- robust scaling 只在训练折拟合；
- MAE/MAPE 混合损失；
- 3 至 5 个随机种子 bagging；
- 与当前集成做折外融合，而不是单模型替换。

深度模型只有在多日滚动验证中稳定超过 LightGBM/CatBoost 才保留。

### 阶段 E：集成方法

当前已经使用线性规划求非负 MAPE 最优权重。下一步应优化泛化，不应继续追求单窗口上的训练误差：

- 权重基于所有折外预测学习；
- 对每个 horizon 单独权重，但向相邻 horizon 和全局权重收缩；
- 用 leave-one-day-out 选择收缩强度；
- 限制单成员最大权重，例如 0.7 至 0.85，除非跨日验证证明单成员稳定占优；
- 对 `generator_1` 和 `generator_all` 分别选权；
- 保持 `0 <= generator_1 <= 200`、`0 <= generator_all <= 440`、`generator_1 <= generator_all`。

在线残差校准目前不稳定，只有在加入工况门控后再复测。门控条件应由起点可见变量决定，例如最近一小时系统偏差、柜位趋势或当前价格档位。

## 5. 现实可达性与 99+ 判定

99+ 是排行榜目标，不等于可以提前保证。当前短周期 MAPE 约为 4.1% 至 5.4%；若平台 49/50 要求两个目标都接近 99% `1-MAPE`，则需要把误差压到约 1%，难度极高。

已有噪声分析提示 15 分钟快变残差可能在 1.7% 至 2.1% 左右，但这只是经验估计，不是严格不可达证明。接手人应完成以下 ceiling analysis：

1. 在 PRE1、PRE2、POST 三个工况分别计算 EMA、rolling median、高斯平滑的残差谱。
2. 用 leave-one-day-out 的最优因果模型估计 h=1..8 的经验误差下界。
3. 检验煤气、气柜、电价等先行变量能解释多少快变残差。
4. 若最强因果模型在多个未见日稳定停在 2% 以上，应把 99+ 的实现路径重新解释为“质量满分 + 官方归一化预测高分”，而不是声称原始 `1-MAPE` 必须达到 99%。

任何时候都不要为了显示 99+ 而改本地评分公式。最终成功只以平台反馈为准。

## 6. 推荐执行顺序

1. 修复模型序列化兼容或完整重训，确保 `validate_outputs.py` 通过。
2. 运行全部单元测试和 smoke test。
3. 用新异常处理文件做一次受控平台提交，先确认 quality=50。
4. 建立 8+ 个 post-regime 日级滚动验证折。
5. 加入电价边界特征并验证。
6. 加入气柜/热风炉/耗气效率的先行特征并验证。
7. 测试目标分解和相似工况 member。
8. 用折外预测重学受收缩的 horizon 权重。
9. 只将通过多日门槛的配置生成正式结果。
10. 再做一次平台受控提交，使用 A/B 结果校准 acc 映射。

## 7. 必跑命令

在项目根目录执行：

```powershell
python -X utf8 -m unittest discover -s tests -p "test_*.py" -v
python -X utf8 tests/smoke_test.py
python -X utf8 experiment.py --output-dir artifacts/development_next
python -X utf8 main.py --official `
  --selection artifacts/development_next/selection.json `
  --output-dir output/official_next
python -X utf8 validate_outputs.py --output-dir output/official_next
python -X utf8 self_score.py output/official_next
```

不要直接覆盖 `output/official/`。先输出到 `output/official_next/`，全部验证通过并比较不退化后再提升为正式目录。

## 8. 每轮实验记录模板

```markdown
### 实验名称

- 假设：为什么这个改动可能降低短周期 MAPE。
- 设计：配置、训练窗口、验证日期、随机种子。
- 防泄漏：每个特征在起点时是否可用，训练标签截止时间。
- 对照：当前冻结模型及其文件 SHA256。
- 结果：两个目标、h=1..8、逐日 mean/std/P90。
- 运行时间：训练、推理、峰值内存。
- 结论：SURVIVED 或 KILLED。
- 后续：合入、继续验证或永久淘汰。
```

没有数字的实验不能进入正式选择。5 月测试窗口的查看次数也必须记录，但绝不能据此选择超参数。

## 9. 最终交付门槛

只有同时满足以下条件才可称为可提交版本：

- 平台或高可信本地复刻中 quality=50/50；
- 新模型在训练内多日滚动验证上稳定优于当前冻结模型；
- `validate_outputs.py` 完整通过，包括模型重载复现；
- 全部单元测试和 smoke test 通过；
- 根目录结果与正式输出目录 SHA256 一致；
- `run_metadata.json`、`validation.json` 的源码哈希与当前代码一致；
- Python 3.10 环境验证通过；
- 平台最终反馈总分达到 99+，才可宣布目标完成。

在达到最后一条之前，所有“99+”都只能写作目标或估计，不能写作已实现结果。
