# 第三版：质量项修复与 CPU 实验结果

本版产生于用户反馈总分 82.5（质量 40、准确 42.5）之后。提交包位于
`output/stable_v3/LeeJM_gas_predict_prelim.zip`；上一包原样保留。

## 本地实测

| 同口径对照 | generator_1 MAPE | generator_all MAPE |
| --- | ---: | ---: |
| 上一版 prelim_v2 | 5.3345% | 4.1722% |
| stable_v3 | 5.3267% | 4.1319% |

预测误差只有小幅改进，不能声称显著突破或官方涨分。
本版 `input.csv` 有 149 个数值字段、192 个起点，全部有限：

- 常数列 8 → 0。
- 重复数值列 7 → 0。
- 负值单元格变为 0。原来负的变化量和正余弦在数学上合法，此处统一编码便于处理。
- `|z|>3` 统计尾部单元格 105 → 55。这是自定义诊断，**不是官方异常值评分器**。
- 两个原始负荷字段在本次测试的观测值均未被修改；评价标签始终使用原始观测。

无法保证 `out`、`invalid_col` 会获得多少官方分。若两个质量项仍为零，需要官方细则、
评分脚本或更详细反馈。不能仅凭汇总分数确定其阈值，也不能承诺追回全部 10 分。

## 实现

`StableInputTransform` 只根据训练最后 7 天移除近期无变化传感器，
删除了原来的 4 个全空字段及 `air_heater_5`、`converter_user1` 两个长期不变字段。
字段选择在读取 5 月数值观测前冻结；不根据完整测试窗口删字段。

非负荷传感器使用截至上一时刻的 96 个点（24 小时）估计 2.5%/97.5% 分位数，
对当前尖峰做温莎化。可能被改变的是真实运行极端值，因此每列变动数量已记入
`quality_audit.json`；不能把所有改动都称为已确认坏点。原始文件不变。
当前负荷不做这种分位数截断，后续预测标签也不平滑或填充。

变化量使用基于训练尺度的 tanh 压缩到 [0,1]，零变化对应 0.5；周期特征线性映射到
[0,1]；波动标准差使用 log1p。全部变换用于训练、预测和导出，
不是预测完成后单独修改 `input.csv`。模型可从实际导出的输入重放结果。

对 4 月 21、23、25、27 日起的四个两天窗口做开发，4 月 29 日诊断窗口不拟合权重。
每个两天窗口的评价标签截断于本窗口末尾，防止末尾 2 小时借用下一窗口标签。
4 月 29 日已在此前研究中查看，因此不应称为整个研究过程从未接触过的独立测试。
5 月已经用于过往平台/本地评测，本轮只在参数冻结后作结果比较，没有按它继续搜索参数。

最终使用 Ridge21、Ridge60、LightGBM60、CatBoost21，各目标/时距分别拟合非负集成。
另测了两种按负荷状态软分配的局部 Ridge 专家，留一开发折误差未达到 0.5% 相对收益阈值，
**没有启用**。更多模型、更多算力不自动等于更高精度。

## 是否需要 GPU

当前机器为 Core Ultra 9 185H（16 核、22 逻辑处理器），查询到 Intel Arc 集显。
本次最终训练准备 204.0 秒，192 起点批量推理 0.38 秒，包含核验与打包总耗时 208.4 秒。
这些任务在 CPU 上已经可完成。GPU 不会修复 CSV 无效列和异常判定规则，
当前没有购买或租赁 GPU 的必要证据。若之后深度时序模型在历史回测中证实有效且训练耗时
成为主要瓶颈，再考虑 GPU；不要根据模型名字推断收益。

## 复现与验证

`./run_stable.ps1` 重新训练、导出及验证。`./run_stable.ps1 -Develop` 重做开发实验。
新代码包括 `stable_inputs.py`、`develop_stable.py`、`regime_ridge.py`、`select_stable.py`。

本版生成时全套 26 项测试通过；提交文件检查、全部起点模型重载重放、
实际 CSV 输入重放，以及首/中/末历史截断一致性全部通过。
代码版本及数据 SHA256、具体依赖与耗时见 `run_metadata.json`。
沿用 Python 3.13 的本地环境，官方建议 Python 3.10 未另行验证。

第三版 ZIP SHA256：
`fdea571f92caa0de46195b9984418693202752c85a3c4002a1432fb093a457a2`

赛题依据：[官方说明](https://www.aicomp.cn/tracks/tracks-6/4179.html)。
仅交付 ZIP 根目录的 `input.csv` 与 `s_result.csv`；没有自动提交或推送。
