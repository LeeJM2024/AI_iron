# V15：以 V10 92.64 为基线的组合稳健化与候选包

## 基线

- 平台已验证最优：V10（`output/adaptive_v10`），总分 92.64，本地平均误差 4.666%（`SUBMISSION_RUNBOOK.md:30`）。
- 目标 93.0 需要平台 acc≥43.0。按已冻结的映射 `acc = 50·(m̄−0.688)/0.312`、`m̄ = 1 − 平均误差`，等价于本地平均误差 ≤ **4.37%**，即需要相对 V10 再降低约 **6%** 的误差。
- 本轮只使用四月折做选择；五月标签仅在候选冻结后做一次对照报告。

## 诊断（四月）

1. **LP 权重拟合存在约 2% 的乐观偏差**：同一权重在折内 5.413%（g1）/3.989%（all），留一窗口评估只有 5.509%/4.077%。也就是说，V10 现役权重方案在诚实口径下的表现差于"不拟合"的组合规则。
2. **误差结构**：集成在 h=1 已达到 15 分钟噪声量级（g1 2.95%、all 1.72%），长步长是误差主体（g1 h=8 7.40%、all 6.61%）。成员分歧度与误差正相关（0.20/0.33），但**分歧越大时集成相对 persistence 的优势越大**（+1.15/+1.44pp），所以"分歧时向 persistence 收缩"的门控方向是错的，未采用。
3. **逐点最优成员上界**：按起点取最优成员可得 g1 3.49%、all 2.78%，但 8 成员最小值本身带有选择噪声，不能视为可达收益。
4. **输入保真度**：分位截断在五月对气柜的最大下修为 −7.5k，四月为 −57k，五月并不更严重；该假设被否定。

## 组合方案 bake-off（3 成员池 × 9 方案，全部四月 LODO）

唯一在**两个目标、调参折与 4/29 留出折四个口径上同时改善**的改动是**给单成员权重设上限**：

| 配置 | g1 调参 | g1 留出 | all 调参 | all 留出 |
| --- | ---: | ---: | ---: | ---: |
| V10 权重（基线） | 5.5091 | 6.0857 | 4.0774 | 6.6624 |
| 上限 0.25，原成员池 | 5.4557 | 6.0827 | **4.0113** | **6.6411** |
| 上限 0.25 + g1 扩池（persistence/ema8/ema16） | **5.3816** | **6.0483** | 4.0855 | 6.6434 |

被否定的方案：等权、中位数、截尾均值（调参折可行但留出折转差或不如上限方案）、无上限 LP、以及若干更激进的收缩。冻结配置：

- generator_1：成员池 = 原 8 成员 + persistence + ema8 + ema16，权重上限 0.25。
- generator_all：成员池不变，权重上限 0.25。
- 上限由四月 bake-off 的"调参+留出相对变化之和"事先确定的规则选出。

## 候选包与冻结后对照

`output/v15_capped/LeeJM_gas_predict_prelim.zip`
（SHA256 `63ec49fc6258dc7d13c435a0a68a88bb48cd45de172b54cbc8c1b6a021e1c3ad`），全部校验通过：
序列化重放、提交输入重放（1e-6）、三点历史截断因果性、行数/列数/有限性/物理边界。
`input.csv` 与 V10 逐字节相同（`ca82523a…`，56 列 = datetime + 55 数值），
因此质量分与 V10 已获验证的 50/50 结构完全一致，本次改动只作用于预测列。

权重实现说明：最初的"单成员权重上限"分摊逻辑在成员数少时会震荡、且裁剪参数化有误，
两种实现给出的四月/五月结果差异都在噪声内（本地 m̄ 差 4e-5）。现已改为标准的
`clip(w−λ, 0, cap)` 投影；在扩展成员池上该上限在各分段均不生效，因此**最终部署权重就是
扩展池上的原始 LP 解**，上限仅作为可选工具保留（见 `select_v15.py` 的 `capped`）。

冻结后本地对照（每步长 MAPE 的 8 步平均，五月标签仅作报告）：

| 模型 | generator_1 | generator_all | 平均误差 | 隐含 acc |
| --- | ---: | ---: | ---: | ---: |
| V10（已提交 92.64） | 5.3652% | 3.9670% | 4.6661% | 42.522 |
| **V15 候选** | **5.3117%** | **3.9635%** | **4.6376%** | **42.568** |
| persistence | 5.9607% | 4.9045% | 5.4326% | 36.1 |

V15 在 8 个步长中的 6 个上优于 V10，预期平台总分约 **92.69**（V10 的本地-平台偏移约 +0.1）。

## 本轮被否定的其它方向

- 上游气侧遥测（23 个被 compact 丢弃的传感器）：同参数对照显示 g1 −0.8% 但 all +1.0%，混合无稳定收益，放弃。
- 步长路径平滑（邻域平均 γ 网格）：γ=0 最优，无收益。
- 用 60 天窗口的简化 pooled 学习器替换/扩池：单成员更强（四月四项指标全胜 pooled_relative），但融入集成后变化在 ±0.5% 噪声内，放弃。
- 样条/平稳化动态家族（V14）：调参折改善、留出折退化，未通过准入。

## V16：恢复气侧轨迹特征（四月大幅改善，五月被本地对照否定）

动机：compact schema 的行内只有气柜/耗气的**当前值**，没有"正在排空/回充"的*轨迹*信息——
`CompactInputTransform` 把非目标字段的 `mean_4/mean_16/change_4` 全部丢弃（见 `compact_inputs.py:48-51`）。
物理先验（气柜斜率与未来负荷变化的相关系数 −0.46/-0.31）没有被模型直接看到。

实现：`compact_plus_inputs.py` 只恢复这 4 个已有传感器的 12 列**既有**因果特征（不新增工程、不新增原始字段）：

- 四月结果（同一批成员、超参、训练窗口，只替换 schema；集成为扩展成员池 + LP 权重）：

| 口径 | g1 原 schema | g1 plus | all 原 schema | all plus |
| --- | ---: | ---: | ---: | ---: |
| 调参折 | 5.4366 | **5.2835**（−2.8%） | 4.0824 | **3.8587**（−5.5%） |
| 4/29 留出 | 6.0364 | **5.9769**（−1.0%） | 6.6437 | **6.4194**（−3.4%） |

**每一个成员、两个目标、两类折都变好**，是本项目至今最强的四月信号。

- 冻结后本地五月对照（`output/v16_plus`）：g1 5.3547%（V10 5.3652，略好），
  **all 4.1009%（V10 3.9670，变差 3.4%）**；`all` 在 h≥60 系统性变差。
- 机制（纯预测诊断，未用标签）：5/1 早间窗口的 2 小时预测跌幅从 V10 的 −1.39% 加深到 −2.43%，
  即模型把气柜快速排空外推成更大的负荷下跌，属于过度反应。
- 结论：**不采用**该 schema 作为提交，保留为"四月改善未迁移到假期窗口"的又一个实例。

## V17/V19：跨工况断点窗口与正则化补救（否定）

为解释 V16 的"四月好、五月差"，新增 **4/17 起始窗口**——它跨越 4/18 工况断点，
是四月里唯一要求模型预测未训练过工况的窗口，比安静窗口更接近假期窗口。

成员级结果（pooled 学习器，5–7 个成员中它权重最高）：

| 窗口 | 目标 | compact | plus 未正则 | plus reg1 | plus reg2 |
| --- | --- | ---: | ---: | ---: | ---: |
| 4/17 断点 | g1 | 7.27% | 7.16% | **6.00%（−17.5%）** | **5.82%（−20.0%）** |
| 4/17 断点 | all | 3.18% | 3.25% | **2.76%（−13.3%）** | **2.60%（−18.2%）** |
| 4/19 | g1 | 4.53% | 4.35% | 4.59% | 4.77% |
| 4/19 | all | 4.17% | 4.20% | **3.88%** | 3.98% |

断点窗口上"轨迹特征 + 强正则化"优势巨大，未正则版本几乎无优势——说明 V16 的问题可能出在容量。

但在**完整成员池**层面（V19，其余成员沿用 V16 缓存，仅替换 pooled 成员）：

| 调参折 LODO | g1 | all |
| --- | ---: | ---: |
| plus 未正则（V16） | 5.2835 | 3.8587 |
| plus reg1 | 5.2934（+0.19%） | 3.9035（+1.16%） |
| plus reg2 | 5.2924（+0.17%） | 3.9248（+1.71%） |

正则化版本在集成层**不优于**未正则版本（4/29 留出折同样混合），因此"用正则化换取迁移性"
这条补救路线也被否定；成员级断点优势没有传导到集成。V17 的七窗口与子集消融脚本
（`tmp_v17.py`、`tmp_v17_ablation.py`、`tmp_v18_grid.py`）保留可复现缓存，未用于生成提交包。

## V20：气柜物理状态成员 + 七窗口（含两个工况突变窗口）

利用早期已缓存的 6 个 `relative_physics` 成员（`phrel*`：气柜水位/斜率/耗尽时间/阈值铰链等
状态特征，全部由已导出的原始列在提交网格上推导，**不需要改 schema**）在七个窗口上评估，
其中 **4/04（清明假期）与 4/19（断点后）** 是两个"工况突变"窗口，权重只在四个安静折上拟合，
因此它们是四月侧最接近五月假期窗口的代理。

预先固定的准入规则：安静折改善 ≥0.5% 相对，且两个突变窗口退化不超过 1%。

| 目标 | 池 | 安静折 | 4/29 | 4/04 | 4/19 | 判定 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| all | V15 池 | — | — | — | — | 基线 |
| all | +物理 | **−2.57%** | **−4.79%** | **−0.81%** | **−2.68%** | 通过 |
| g1 | +物理 | −1.61% | +0.01% | +3.17% | **+13.01%** | 否决 |

`generator_all` 由此采用物理池（LP 把 0.54/0.56 权重给了 `phrel60_6h_mae`），`generator_1`
保持 V15 冻结权重；`input.csv` 与 V10 逐字节相同（`ca82523a…`）。

**冻结后本地五月对照**：g1 5.3117%（与 V15 相同）、all 3.9845%（V15 3.9635%），
即**四月四类窗口全部改善的池在五月反而略差 0.5%**。包位于 `output/v20_physics`（`eb3270b3…`），
校验全部通过。这是本项目中第二次出现"四月一致改善、五月不迁移"（V16 为第一次，幅度更大）。

逐步长看，V20 在 h=15–75 全面优于 V15/V10，但在 h=90–120 变差（all：4.992/5.190/5.079
对 V15 的 4.969/5.037/4.977）。与 V16 的签名一致：**在五月窗口里，额外的气侧状态信息
让 ≤1 小时的预测变好、让 2 小时的预测变差**。这是五月标签下的机制观察，仅用于报告，
不用于任何选型。

## 结论与提交建议

- 两个可直接提交的候选，均与 V10 的 `input.csv` 逐字节相同（质量分零风险）：
  - **V15**（`output/v15_capped`，`63ec49fc…`）：本地平均误差 4.6376%，预期约 **92.69**；
    改动仅为成员池扩展（persistence/EMA 进入 g1），四月两端口径一致。
  - V20（`output/v20_physics`，`eb3270b3…`）：本地 4.6481%，预期约 92.66；
    `all` 在四月的四类窗口全部改善，但本地五月略差。
- 我已按四月预注册规则把 V20 判定为通过；两者之间的取舍若参考本地数字，即属于用五月标签
  选型，这一步按项目规则应由你决定，我只报告数字。
- 93.0 需要相对误差再降约 6%。本轮共 V14–V20 七个实验族，结论一致：
  **四月（含清明与断点窗口）的改善无法迁移到 5/1–5/2**，能迁移的只有 +0.05 量级的成员池微调。



