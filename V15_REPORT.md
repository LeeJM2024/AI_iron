# V15：当前最优方案（平台实测 92.6879）

本文件是项目的**唯一现行报告**：V15 方案、本地↔平台校准、93 门槛、以及本轮全部否定实验的
结论清单（被否定实验的代码与缓存已删除，只保留结论）。

## 1. 当前基准

| 包 | 本地平均误差（5/1–5/2 口径） | 平台总分 |
| --- | ---: | ---: |
| `output/adaptive_v10`（更早的基线） | 4.6661% | 92.64（实测） |
| `output/v15_capped`（平台已验证的锚） | 4.6376% | 92.6879（实测） |
| **`output/v30_bundle`（当前最优，待提交验证）** | **4.6265%** | 预计 **92.706** |

- 平台映射（已两点验证）：`总分 = 50 + 50×(0.312 − (本地误差 − 0.00074))/0.312`，
  即 **0.01pp 本地误差 ≈ 0.016 分**，1 个 acc 分 = 0.625pp 误差。
- **93.0 的门槛：本地平均误差 ≤ 4.442%**（V30 为 4.6265%，还差 0.185pp = 4.0% 相对）。

### V30 = V15 + 两个成员超参（9 个安静窗口配对验证）

把权重拟合窗口扩到 9 个安静窗口（4/20–4/28）后，逐个成员做配对比较（同窗口同折、每折只用
cut-off 前的数据）：

| 旋钮 | generator_1 | generator_all | 采纳 |
| --- | --- | --- | --- |
| `relative60` 岭正则 300 → 600 | **9/9 窗口改善**（−0.71%，sd 0.34） | **8/9**（−0.27%，sd 0.27） | ✅ |
| `lgb60` 时间半衰期 14 → 21 天 | 5/9（−0.14%） | 7/9（−0.80%） | ✅（随 α 一起打包） |
| `pooled` 衰减/容量（hl10、hl30、800 树、60 天） | 4–5/9 | 5/9 | ❌ 噪声 |
| `lgb` 其它（hl07、hl30+400 树、90 天） | 5/9 | 6–7/9 | ❌ |
| `relative60` hl07 / hl30 / α100 | 4/9、4/9 等 | 0/9（hl30 明显变差） | ❌ |

V30 相对 V15：本地 g1 5.3117% → **5.2960%**，all 3.9635% → **3.9570%**，平均 4.6376% → **4.6265%**；
`input.csv` 与 V10/V15 逐字节相同，全部审计通过。只改 relative60 的 V29（本地 4.6356%）被 V30 支配，已删除。

### V31–V33：最强四月信号也无法迁移（结论性）

把同一配对协议推广到其余有权重成员，并直接检验"漂移"假设：

| 实验 | 四月配对结果 | 结论 |
| --- | --- | --- |
| `relative21` 岭正则 100 → 2000 | **g1 9/9、all 9/9 窗口改善；成员级 −9.6% / −5.4%（全项目最强、最一致的信号）** | 打包为 V32 后本地门槛 **4.6525%**（V30 为 4.6265%）：g1 5.2926% 略好，但 **all 4.0123% 差 1.4%** → **否决并删除** |
| `online7_6h` α 300→600 | g1 6/9、all 7/9（−1.5% / −2.2%） | 与 V32 同批打包，未单独采用 |
| `online21_2h` α 100→300 | g1 3/9（+0.3%）、all 8/9（−1.5%） | 目标间符号不一致，不采纳 |
| `relative60_mae` / `relative14_mae` 的 α 与衰减 | 改善窗口 1–4/9 | 噪声，不采纳 |
| 纯漂移成员（每步长加权中位数、无特征） | g1 5.824% / all 4.723% | ≈ persistence（5.831% / 4.742%），无价值 |

**机制笔记**：这些相对特征高度共线，低 α 时拟合成不稳定的特征组合；提高 α 后成员退化为
"persistence + 小而稳定的修正"（即唯一能跨窗口迁移的那类成员形态）。但即使如此，
它在 5/1–5/2 上仍不改善。

**含义（重要）**：四月折——包括 9 个安静窗口的逐窗口配对比较——对 **5/1–5/2 这一个特定窗口**
几乎没有选择功效：连 9/9 一致、幅度 −9.6% 的成员级改进都不迁移。这与"窗口难度主导分数"
（共同规律②）一致：模型在任意给定窗口上的表现主要由该窗口自身的可预测性决定，
四月与五月的差异足以淹没所有 ≤10% 的改进。

## 2. V15 方案

改动只有一处，且**不触碰输入结构**：

- `generator_1` 的成员池在原有 8 个成员之外加入 `persistence`、`ema8`、`ema16`；
  `generator_all` 池不变。全部集成权重在**四个四月调参折**上用 MAPE 对齐的凸 LP 重拟
  （无权重上限：扩展池下各分段权重均已低于 0.25，上限为无操作；`select_v15.py` 里的
  `capped` 是修正后的标准投影，保留备用）。
- 冻结配置：`artifacts/v15_selected/selection.json`；生成脚本：`select_v15.py`。
- `input.csv` 与 V10 **逐字节相同**（`ca82523a…`，datetime + 55 数值列，
  无缺失/常数/重复列）→ 质量分与已验证的 50/50 结构一致，改动只作用于预测列。

四月证据（同一协议下的诚实口径）：

| 口径 | V10 池 | V15 池（部署配置） |
| --- | ---: | ---: |
| 调参折 LODO g1 / all | 5.5088 / 4.0774 | **5.4366** / 4.0824 |
| 4/29 留出 g1 / all | 6.0857 / 6.6624 | **6.0364** / 6.6437 |
| 五折 g1 / all | 5.6242 / 4.5944 | **5.5566** / 4.5947 |

即：`generator_1` 在两个口径上都改善（−1.3% / −0.8%），`generator_all` 持平；
冻结后本地五月口径两个目标同时改善（4.6661% → 4.6376%），平台分 +0.048。

## 3. 诊断结论（四月，供后续参考）

1. LP 权重拟合有约 2% 的乐观偏差：折内 5.413% vs 留一 5.509%（g1）。故权重必须用 LODO 评估。
2. 集成在 h=1 已达 15 分钟噪声量级（g1 2.95%、all 1.72%），长步长是误差主体（h=8 7.40%/6.61%）。
3. **成员分歧越大时集成相对 persistence 的优势越大**（+1.15/+1.44pp）→ "分歧时向 persistence 收缩"方向错误。
4. 集成对近期趋势的响应**已在最优点**：最优趋势修正系数 γ≈0（h=6–8 甚至为负），
   施加修正在三个留出窗口上全部变差。
5. 输入保真度：分位截断在五月对气柜最大下修 −7.5k，四月 −57k，五月并不更严重。

## 4. 否定清单（代码与缓存已删除，仅留结论）

| 实验族 | 四月表现 | 五月/留出表现 | 结论 |
| --- | --- | --- | --- |
| 平稳化 / 样条动态（V14） | 调参折改善（all −5.5%） | 4/29 留出退化 | 未通过准入 |
| 气侧轨迹 schema（V16，+12 列） | **每个成员、两个目标、所有折全改善**（−2.8%/−5.5%） | all 反转 +3.4%，h≥60 系统性劣化 | 否决；机制＝把气柜排空外推成更大跌幅 |
| 正则化补救（V17/V19） | 断点窗口成员级 −13%~−20% | 集成层变差 0.2%~1.7% | 否决 |
| 气柜物理成员池（V20，all） | 四类窗口全改善（−2.6%/−4.8%/−0.8%/−2.7%） | 本地 4.648% 略差于 V15 | 不晋级 |
| 更长训练窗口（V21，120/150 天） | 4/21 折 g1 −1.5% / all +3.9% | — | 不一致，放弃 |
| 轨迹曲率外推（V23） | 未完成 | — | 放弃 |
| 上游 23 个被丢弃传感器 | 同参数对照：g1 −0.8% / all +1.0% | — | 混合无收益 |
| 成员分歧门控、步长路径平滑、跨 schema 对冲、简化 pooled 替换、多任务共享模型 | 均无稳定收益 | — | 放弃 |
| 阻尼回归/滤波锚点成员族（V24，rev8/16/32/96 + 双滤波） | g1 权重为 0；all quiet +0.31% | all 三个窗口 +0.06~+1.69% | 否决 |
| 六窗口权重拟合（V25：quiet4+清明+断点） | 跨窗口迁移 −0.7%/−0.7% | **本地 4.680% 差于 V15 4.638%** | 否决（未过晋级门槛） |
| 九安静窗口权重拟合（V26：+4/20–4/28） | g1 中性、all +0.27% | — | 否决 |
| GRU 序列成员 | 每折约 12 分钟、全程约 3.5 小时 | — | 因成本与低先验中止 |

**共同规律①：四月（含清明与断点窗口）的改善无法迁移到 5/1–5/2；唯一迁移成功的是 V15 的成员池扩展。**

**共同规律②（本轮新增，重要）：窗口难度主导分数。** 同一套 V15 模型在不同两日窗口上的
平均误差是 3.23%（4/26）到 5.79%（4/21），相差 1.8 倍；而所有配置差异都只有 ~1%。
5/1–5/2 的本地误差 4.638% 低于全部四月窗口（4.76–5.79%），即**五月窗口偏"容易"，
表现更接近安静窗口而非工况突变窗口**。这解释了为什么"为突变窗口优化"的三次尝试
（V16/V20/V25）全部让五月分变差，也说明在现有信息集下，V15 已接近该窗口的可达范围。

**共同规律③（结论性）：长步长已在上限。** 93.0 的门槛若靠长步长突破，需要
h=90/105/120 的误差降低约 20%。做七路对比（April LODO，5 窗口：现役集成、3 个最强单成员、
高容量直接学习器 1200 树/31 叶、两个堆叠元学习器——输入为成员相对预测 + 起点状态特征
（波动/时钟/气柜状态/成员分歧））：

| 目标 | 步长 | V15 集成 | 最强替代 | 差距 |
| --- | ---: | ---: | --- | ---: |
| generator_1 | h6 / h7 / h8 | **6.517 / 6.923 / 7.211** | direct_hc 6.664 / 7.037 / 7.461 | +2.2% / +1.7% / +3.5% |
| generator_all | h6 / h7 / h8 | **5.751 / 6.195 / 6.582** | stack_small 5.763 / 6.348、relative60_mae 6.733 | +0.2% / +2.5% / +2.3% |

结论：**长步长上现役集成在每一格点都是最优**，比最强单成员好 3.6–6.4%，比"更一般"的
堆叠元学习好 0–7%，比高容量直接模型好 2–14%。即长步长要再降 20% 需要比现有全部模型族
好 20–30% 的新模型，在本信息集下不存在（脚本 `tmp_v27_long.py` 可复现；五月未读）。

## 5. 复现

```powershell
python -X utf8 select_v15.py                       # 生成 artifacts/v15_selected/selection.json
python -X utf8 -W ignore::DeprecationWarning run_prelim.py `
  --train-dir "煤气发电预测优化-初赛训练和测试集/初赛-参赛者使用" `
  --test-dir  "煤气发电预测优化-初赛训练和测试集/初赛-评分所用测试集" `
  --selection artifacts/v15_selected/selection.json --output-dir output/v15_capped
python -X utf8 -m pytest tests -q                  # 55 passed
```

`select_v15.py` 需要 `artifacts/{compact_v5,online_v10,relative_linear_v10}` 的四月折缓存
（可由 `develop_compact.py` 等重新生成，按仓库约定不入库）。`output/` 同样按约定不入库，
但重新生成的 `input.csv`/`s_result.csv` 与已提交版本逐字节一致（已验证）。

## 6. 版本迭代规则

见 `SUBMISSION_RUNBOOK.md` 第七节：**每个新版本必须严格优于当前最优（本地 4.6376% / 平台 92.6879）
才允许命名与提交**；开发期只在四月数据上选择，冻结后的本地口径仅用于晋级判定；
被否定的候选删除代码与缓存，只在本文件第 4 节留结论。
