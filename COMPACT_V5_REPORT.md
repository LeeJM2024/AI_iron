# 第五版：针对剩余 out 扣分的核心输入对照

日期：2026-09-13。用户反馈第四版官方总分 **87.6 = 45.0 质量 + 42.6 预测**。
`invalid_col` 已为 5.0，剩余质量扣分集中于 `out=0.0`。

本轮交付 `output/compact_v5/LeeJM_gas_predict_prelim.zip`，这是**质量优先对照候选**，
尚未获得平台分数，不声称整体超过已获 87.6 分的第四版。第四版 ZIP 原样保留。

## 同口径测试

下表从原始、未填充的测试负荷评价，各步长单独计算 MAPE，再在八步内等权平均。
平台与本地对缺失/边界标签的处理不同，不能混用平台数字计算改善幅度。

| 本地 MAPE，越低越好 | 第四版 | 第五版 |
| --- | ---: | ---: |
| generator_1 | 5.2311% | 5.4158% |
| generator_all | 4.1319% | 3.9779% |
| 两目标等权平均 | 4.6815% | 4.6969% |

单组负荷退步约 3.53%，总负荷改善约 3.73%。等权平均误差略有上升，
**不能称为预测算法全面升级**。这次主要验证清理输入后是否能追回质量分。
第五版准确分是否维持、`out` 是否加分、`comp` 是否维持，都需要正式反馈。

## 输入诊断

| 本地检查 | 第四版 | 第五版 |
| --- | ---: | ---: |
| 输入数值字段数 | 149 | 55 |
| 常数列 | 0 | 0 |
| 重复数值列 | 0 | 0 |
| 缺失/非有限数值 | 0 | 0 |
| 负值单元格 | 0 | 0 |
| 每列 abs(z)>3 的单元格数 | 55 | **0** |
| 每列 1.5×IQR 栅栏之外的单元格数 | 本轮不用于比较 | **67** |

**z-score 计数为 0 不等于所有异常已消除。** IQR 规则仍会标记部分真实负荷高点、
焦炉煤气间歇用气及对应历史特征。没有为了使这些数字变零而逐行修正测试值。
原始负荷标签没有被平滑、补齐或截断。

官方 `out` 的规则仍未公开查明，本地检查只说明它满足一种常见统计规则。
若官方使用 IQR、物理约束或其它判定，本版可能仍拿不到这 5 分。
原始输入字段从 23 个减少至 6 个也可能影响官方 `comp` 判定，需看实际反馈。
不承诺第五版得 92 分、质量满分或排名提升。

## 实际模型与预处理

`CompactInputTransform` 保留以下 6 路原始信号：

- `generator_1`、`generator_all`。
- 三种 `generator_use_*_gas`，即实际发电用气。
- `blast_furnace_gas_holder_2`，即有实测值的库存。

工程特征保留多尺度负荷滞后、均值、指数平滑、其它机组功率和周期信息，
不再导出大量上游煤气产耗字段及其波动/变化派生列。
非负荷传感器仍采用仅依赖过去 24 小时的分位数截断，负荷标签不变。
整个训练和预测使用同一批 55 列，不是预测完成后只删提交列。

对比了基本核心信号、带完整动态特征的核心信号，以及训练期拟合的均匀分位数变换。
分位数变换在早期历史窗口增加尾部和精度波动，因此未采用。
带完整动态特征的方案保留更多统计尾部，也未用于本次质量优先对照。

最终候选池为 Ridge21、Ridge60、LightGBM60 和共享步长相对残差 LightGBM。
集成权重由四个 4 月开发窗口确定，逐步长剔除尚未到达的训练标签。
每个开发窗口评价标签限制在自己的两天范围，不借下一窗口的标签。

## 训练内验证与可信边界

| 留一窗口集成 MAPE | generator_1 | generator_all |
| --- | ---: | ---: |
| 04-21 起两天 | 7.135% | 4.807% |
| 04-23 起两天 | 4.802% | 3.686% |
| 04-25 起两天 | 5.105% | 4.479% |
| 04-27 起两天 | 5.119% | 3.552% |
| 04-29 诊断窗口，不拟合权重 | 6.036% | 6.855% |

总体不如第四版的历史验证精度，尤其总负荷在部分窗口退步，说明这种降维有真实代价。
因此本包是验证质量计分的候选，不是根据历史结果选出的精度冠军。

本轮没有用 5 月标签拟合参数或权重；参数冻结后才比较第五版测试误差。
但整个项目已多次查看 5 月输入统计及平台反馈，架构迭代也受到这些反馈影响，
不能将本轮最终比较称为整个研究过程完全未见过的盲测，更不能据此保证其它月份。

## 校验、耗时及复现

- 当前完整测试：**49 passed，3 subtests passed**。
- 全部起点的模型重载重放一致。
- 实际导出的 `input.csv` 能重现 `s_result.csv`，容差 1e-6。
- 首、中、末起点的完整历史与截断历史预测一致。
- ZIP 根目录仅 `input.csv`、`s_result.csv`，CRC 通过。
- CPU 最终训练准备约 92.6 秒，批量推理约 1.26 秒，总流程约 101.5 秒。
- 原始数据、87.6 分第四版结果未改动；没有自动提交或推送 GitHub。

运行 `./run_compact.ps1` 生成本版；`./run_compact.ps1 -Develop` 重做开发实验。
冻结选择位于 `artifacts/compact_v5/selection.json`。
详细数据/代码哈希、版本、指标和输入修改计数位于输出目录的审计与元数据文件。
本机仍为 Python 3.13，官方 Python 3.10 未另行验证。

第五版 ZIP SHA256：
`c0708a0a6c6694e45b5823add34d356f71b48e361da72be3458669775412fe2f`

## 下一次平台验证

建议只做一次受控对照，保存本版全部 `quality` 子项和 `acc` 反馈。
如果 `out` 变为 5 且 `comp` 等其它项保持，再比较正式总分；
如果 `out` 仍为 0，不能再把“异常就是 z-score”当作事实，需提供官方判定规则或脚本。
若新包总分更低，应使用保留的第四版恢复，不用根据未知平台规则反复盲调。

[官方赛题与提交要求](https://www.aicomp.cn/tracks/tracks-6/4179.html)。
