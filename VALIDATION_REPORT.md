# 实测验证报告

验证日期：2026-09-11。数据来自项目内主办方提供的初赛训练和评分测试包。最终模型、预测、调度及独立核验结果已保存至 `output/official/`。

## 真实测试结果

训练数据保留到 **2025-04-30 23:45**，测试为 **2025-05-01 00:00 至 2025-05-02 23:45**，共 **192 个滚动起点**。每个起点使用当时及之前已到达的观测，模型参数在读取测试观测之前已冻结。没有用测试分数重新调参。

下表先计算各步长的 MAPE，再在对应 8/96 步内等权平均。持续值基线是每个起点的最新已知真实负荷，缺测时仅前向填充历史输入。评价标签始终来自未填充、未平滑的原始观测。

| 周期 | 目标 | 持续值 MAPE | 本方案 MAPE | 本方案 1−MAPE | 相对误差下降 |
| --- | --- | ---: | ---: | ---: | ---: |
| 2 小时，8 步 | generator_1 | 5.9607% | **5.6342%** | 94.3658% | 5.48% |
| 2 小时，8 步 | generator_all | 4.9045% | **4.2327%** | 95.7673% | 13.70% |
| 24 小时，96 步 | generator_1 | 8.7216% | **7.8316%** | 92.1684% | 10.07% |
| 24 小时，96 步 | generator_all | 7.0188% | **5.3483%** | 94.6517% | 23.80% |

这四项结果都优于同协议持续值基线。没有官方评分程序和其他队伍成绩，因此不能把这些数值直接称为官方总分、接近满分或排行榜前列。

## 覆盖范围与限制

测试包只有两天，无法代表全季节及所有停机/切换工况。192 个起点全部输出预测，但末尾起点的部分目标时刻超出测试集；这些未知标签不评分，也没有通过填充构造真实值。测试内 `generator_1` 缺 2 个观测，`generator_all` 缺 1 个观测，没有零值标签。

| 周期/目标 | 可评分格子 | 总预测格子 | 不可评分格子 |
| --- | ---: | ---: | ---: |
| 8 步 / generator_1 | 1,484 | 1,536 | 52 |
| 8 步 / generator_all | 1,492 | 1,536 | 44 |
| 96 步 / generator_1 | 13,584 | 18,432 | 4,848 |
| 96 步 / generator_all | 13,680 | 18,432 | 4,752 |

各步长评分样本数不同，完整明细见 `test_metrics_by_horizon.csv`。按天明细先在每个起点的可评分步长内求平均，再按天汇总，与上表步长等权口径不同，不应直接平均日表来复现上表。由于第二天远期标签不足，也不宜据此声称第二天长周期更容易。

本协议允许每 15 分钟接收新观测。若官方要求从同一初始时刻封闭预测整个测试区间，不允许期间使用新观测，必须重新实施该协议并重新评分。

## 历史选择及未参与选择的验证

模型采用逐步长残差回归、逆负荷加权 MAE、LightGBM / CatBoost，以及持续值、指数平滑、日周期候选。`generator_1` 的最终训练加入 30 天半衰期时间权重，`generator_all` 保持不加衰减。集成权重非负且总和为 1，按目标和步长区间分别选择。

两个开发窗口起于 4 月 7 日、18 日，各使用两天滚动起点。4 月 27 日的两天窗口只用于验证，不参与集成权重选择。开发长周期抽样第 16、24、48、96 步；最终训练、导出与真实测试使用完整 1–96 步，没有用插值代替正式逐步预测。

| 4 月独立验证 | 目标 | 持续值 MAPE | 选定集成 MAPE |
| --- | --- | ---: | ---: |
| 8 步 | generator_1 | 5.7598% | 5.0801% |
| 8 步 | generator_all | 4.0854% | 3.4171% |
| 长周期抽样四步 | generator_1 | 11.8352% | 10.6993% |
| 长周期抽样四步 | generator_all | 12.6198% | 10.5644% |

完整记录保存在 `artifacts/development/`。集成并非所有窗口都优于每个单模型，选择过程只能支持其在已有历史和本次测试上的表现，不能证明在新工况必然占优。远期 `generator_1` 更偏向平滑预测，避免无验证收益的复杂模型占用计算资源。

## 运行质量

| 项目 | 实测 |
| --- | ---: |
| 开发实验，三折 | 509.0 秒 |
| 最终训练及模型准备 | 532.2 秒 |
| 192 起点 × 两目标 × 96 步推理 | 3.510 秒 |
| 平均每起点批量推理耗时 | 18.3 毫秒 |
| 正式全流程，不含随后独立核验 | 538.2 秒 |
| 保存的模型 | 24,265,508 字节，约 23.14 MiB |

平均批量耗时不是在线单请求延迟 SLA。训练在 CPU 上完成，依赖版本见 `validation.json`。本机 Python 3.13.5 已跑通，赛题允许的 Python 3.10 环境仍需上线前验证，不能用本机运行替代官方容器检查。

已通过 11 项自动测试：右闭重采样、缺失标签保留、未来扰动下的特征不变、未来扰动下的模型训练不变及序列化一致、共享步长模型防泄漏、时间衰减模型配置、电价跳变与缺失电价失败、96 步整数调度、库存越界拒绝、优先用户资源缺口拒绝。另有端到端脏数据冒烟测试通过。

`validate_outputs.py` 独立读取导出 CSV，检查字段顺序、行数、唯一时间戳、有限数值、短/长表一致、JSON 一致，并从原始测试标签复算评分。从实际交付模型重新预测首个、中间、最后起点，全部 192 个预测字段与 CSV 在 1e−6 容差内一致。

## 调度验证

调度区间为最后测试起点之后的 24 小时。采用观测到的 **2 号高炉煤气柜，容量 300,000 m³**；1 号柜全缺失，未推造库存。四台 50MW、两台 120MW 机组的在线数量为整数，允许停机，在线机组运行在 60%–100% 额定范围内。

| 指标 | 结果 |
| --- | ---: |
| 安全库存范围 | 45,000–270,000 m³ |
| 本次库存最小/最大 | 45,000 / 270,000 m³ |
| 末端库存 | 136,183.48 m³，不低于初始 |
| 最大总功率 | 367.5964 MW，低于 440 MW |
| 预测资源下放散量 | 0 m³ |
| 虚构应急补气 | 0 m³ |
| 求解器最大约束残差 | 1.88e−12，内部缩放变量单位 |
| MIP 相对 gap | 0.1765% |
| 分时电价计划收入 | 4,327,748.04 元，按电价为元/kWh 假设 |
| 同资源平价排程按真实电价计算收入 | 4,229,123.71 元 |
| 本次仿真增收 | 98,624.34 元，约 2.33% |

求解器达到设定 0.5% gap 容差并报告 optimal，这不等于证明数学意义上 gap 为零。收入比较的基线是同一资源、机组和末端库存条件下，用常数电价求出的排程，再以真实分时电价计价；不是电厂真实历史排程，也不是已实现收益。

CSV 独立复算通过了逐类煤气守恒、无跨气种借量、气电转换、出力/在线台数约束、库存安全及末端回补、收入计算。资源边界和转换系数保存在 `dispatch_inputs.csv`，执行路径保存在 `dispatch_audit.csv`。

数据字典未明确流量单位，当前假设发电用气为 m³/h。优先用户供气后的资源由历史发电用气和库存变化估计，采用最近观测稳健水平。未观测柜库存、真实未来供需、动态热值、机组初始状态、最小开停机时间及现场限制尚未完整建模。以上收益和约束成立范围是该资源预测及模型假设，不能解释为现场可直接兑现的调度保证。

## 复现

在项目目录运行 `python -X utf8 main.py --official --output-dir output/official`，或执行 `run.ps1`。当前冻结选择包含 `generator_1` 的 30 天半衰期权重；完整重做开发实验使用 `python -X utf8 experiment.py --recency-half-life 30 --recency-targets generator_1`。只复查现有交付文件使用 `python -X utf8 validate_outputs.py`，无需重新训练。

后续改进应优先补充更多未见测试窗口、气量单位及现场约束。任何新的算法或超参数搜索都应返回训练内滚动验证，不能继续根据本次已查看的两天测试结果选模型。
