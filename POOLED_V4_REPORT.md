# 第四版交付与实测（2026-09-12）

推荐本轮下一次提交使用 `output/pooled_v4/LeeJM_gas_predict_prelim.zip`。
它合并第三版的输入预处理与训练内验证通过的共享步长模型。
用户此前 82.5 分的第二版包和第三版包均原样保留。

## 同口径比较

本表使用原始未填充的测试标签，每个步长计算 MAPE，再在八个步长内平均。
缺失标签、超出测试边界的标签不评价。数值与平台反馈口径存在差异，
所以只对本地文件用同一函数比较，不拿平台数字和本地数字混算改善幅度。

| 版本 | generator_1 MAPE | generator_all MAPE |
| --- | ---: | ---: |
| 已提交的第二版 prelim_v2，本地复算 | 5.3345% | 4.1722% |
| 第三版 stable_v3 | 5.3267% | 4.1319% |
| 第四版 pooled_v4 | **5.2311%** | **4.1319%** |

相对第二版，两目标误差下降约 **1.94%、0.97%**。这是小幅真实改善，不是精度大突破。
平台已经反馈第二版 `quality=40/50, acc=42.5/50`，
但没有反馈第四版；**第四版的官方总分、质量分和排名均未知**。

## 质量项修改

第四版输入与第三版逐字节相同，预处理方法、限制和原始字段变动说明见
`STABLE_V3_REPORT.md`。相比第二版：

| 本地输入诊断 | 第二版 | 第四版 |
| --- | ---: | ---: |
| 数值字段数 | 157 | 149 |
| 常数列 | 8 | 0 |
| 重复数值列 | 7 | 0 |
| 缺失值 | 0 | 0 |
| 非有限数值 | 0 | 0 |
| 负值单元格 | 存在于有意义的变化量/周期特征 | 0（等价或稳健编码） |
| abs(z)>3 统计尾部单元格 | 105 | 55 |

`out`、`invalid_col` 的正式定义仍未知。
**没有解决或证明消除全部“异常值”**，55 个统计尾部不能自动等同于坏点，也不能
直接换算官方得分。不能承诺质量从 40 变成 50。
若第四版提交后两项仍为 0，需要官方脚本/阈值/详细反馈，继续靠汇总分数猜测难以定位。

所有改动进入真实训练和推理。没有单独修改提交输入来美化质量分，也未人工逐行修改预测。

## 共享步长算法

新增 `PooledShort`，将 1–8 步作为同一模型的查询条件，两个目标分别拟合。
模型额外构造当前负荷归一化的滞后/平滑负荷、煤气量与功率比例、目标时刻正余弦。
这些派生量都能由导出的输入行与已知查询步长重算，不访问目标时刻的观测。

相对变化目标为 `(y(t+h)-y(t))/max(y(t),1)`，预测时恢复到原始 MW。
对应样本权重为 `max(y(t),1)/max(y(t+h),1)`，使加权绝对误差与 MAPE 对齐，
再乘仅由训练样本年龄计算的 21 天半衰期权重。
每个步长单独剔除晚于训练截止时间的标签；所有步长样本共同训练 LightGBM。

比较 21 天、60 天绝对残差模型与 90 天相对残差模型。
最终非零权重只接受相对残差模型，并且**只用于 generator_1**。
总负荷保留第三版的组合；局部工况模型与不稳定的共享模型没有强行启用。

## 训练内证据

四个开发窗口分别起于 4 月 21、23、25、27 日，每个两天。
每个窗口的评价标签截止于本窗口结束，避免跨窗口借标签。
权重层使用留一窗口交叉验证，最后 4 月 29 日窗口不拟合权重。
它已在之前研究中查看，不能称为完全未接触过的独立测试。
5 月在本轮模型选择中完全不使用，仅在参数冻结后作最终文件比较。

| 四个开发窗口留一折平均 MAPE | 第三版候选池 | 加共享模型的候选池 | 决定 |
| --- | ---: | ---: | --- |
| generator_1 | 5.3796% | 5.3476% | 改善约 0.59%，超过 0.5% 阈值，接受 |
| generator_all | 3.9285% | 3.9313% | 未改善，拒绝 |

不能把平均改善解释为所有窗口改善：4 月 23 日 `generator_1` 的留一折结果退步。
4 月末诊断窗口，该目标由 6.1763% 改善至 6.0414%。
最终 5 月误差改善仍较小，工况突变和训练分布漂移依旧是主要困难。

## GPU 与耗时

本轮全部在 CPU 完成，不依赖 GPU，也没有安装深度学习框架或调用外部模型服务。
最终新增相对残差模型训练约 52.2 秒，复用四个已核对数据、配置与特征的第三版模型；
包含重载、历史截断、CSV 重放、打包及评分，约 68.6 秒。批量推理约 1.38 秒。
这个时间不包括此前候选开发，也不等于全部模型从零训练耗时。

当前优先级应是确认官方质量规则，而不是购买 GPU。将来如深度时序模型在严格历史
回测中证实收益，且训练算力成为瓶颈，再考虑 GPU；现有结果不支持先买卡。

## 校验与复现

- 全套 29 项测试通过。随后增加了复用模型校验，相关 16 项测试再次通过。
- 模型序列化后所有起点预测一致。
- 从实际 `input.csv` 重放 `s_result.csv`，误差小于 1e-6。
- 首、中、末起点在完整历史与截断历史下输出一致。
- ZIP 根目录恰好 `input.csv` 和 `s_result.csv`；CRC 检查通过。
- 上一包未被覆盖；没有自动向平台提交或推送 GitHub。

运行 `./run_pooled.ps1` 可从冻结选择重训和导出，
`./run_pooled.ps1 -Develop` 可重做候选实验。
Python CLI 入口为 `run_prelim.py --selection artifacts/pooled_v4_selected/selection.json`。
数据路径、依赖版本、代码哈希、数据哈希、耗时和模型复用成员记录于输出目录元数据。
当前依然是 Python 3.13 的本地验证；官方 Python 3.10 尚未另行验证。

新提交包 SHA256：
`6a043072ffa98a8d17954be8ee370987df3bd5864d18ff1300899d180ae8947f`

[官方赛题与提交格式](https://www.aicomp.cn/tracks/tracks-6/4179.html)。
