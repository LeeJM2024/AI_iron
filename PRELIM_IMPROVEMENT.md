# 初赛优化与实测结果（2026-09-12）

## 交付结论

新提交包：`output/prelim_v2/LeeJM_gas_predict_prelim.zip`。
它已通过文件格式、完整性、模型重载、导出输入重放及历史截断一致性核验。
ZIP 根目录只有 `input.csv` 和 `s_result.csv`，符合当前公开的初赛提交格式。
没有替换仓库根目录的旧预测，没有自动提交平台，也没有推送 GitHub。

来源：[官方赛题及提交要求](https://www.aicomp.cn/tracks/tracks-6/4179.html)。

## 同口径测试比较

使用官方测试包中未填充、未平滑的原始负荷标签。每个步长单独计算 MAPE，
再对 8 个步长等权平均。缺失标签和超出测试边界的目标不评分，不伪造真值。
5 月没有参与候选选择、权重拟合或异常阈值拟合。

| 同一测试数据、同一评价函数 | generator_1 MAPE | generator_all MAPE |
| --- | ---: | ---: |
| 当前值持续基线 | 5.9607% | 4.9045% |
| 上一级本地旧结果 `_real_prelim_final/s_result.csv` | 5.7517% | 4.7847% |
| 拉取的 GitHub 最新版根目录 `s_result.csv` | 5.4268% | 4.1020% |
| 本次新方案 | **5.3345%** | **4.1722%** |

新方案的两个 1−MAPE 分别为 **0.946655、0.958278**。
相对本地旧结果，两目标误差分别下降约 **7.3%、12.8%**。
相对 GitHub 最新版，单组负荷误差下降约 1.7%，总负荷误差上升约 1.7%。
两目标平均误差仅改善约 0.23%，因此**不能声称全面、大幅超越最新仓库**。

用户反馈的 81.7 分及两个 1−MAPE 与上述本地文件并不完全对应，
没有原始已提交 ZIP/官方评分脚本，不能声称完全复现该次平台评分。
平台 `acc=41.7/50` 也不等于两个 1−MAPE 平均后直接乘以 50。
这里不反推或捏造官方总分，更不承诺名次。

## 实际改动

1. 新增只负责初赛 15、30、45、60、75、90、105、120 分钟的独立流程。
   不再为初赛训练 96 个长周期步长或求解发电调度。
2. 保留评价标签，不对真实负荷的工况变化做 Hampel 平滑。
   输入只按历史拟合的宽松传感器界限及已知物理上界屏蔽异常，再前向填充。
3. 删除训练中完全缺失的 4 个传感器，不把未知测量伪填成有效全零传感器。
   数值输入从本地旧版 588 列减少为 **157 列（25 原始字段、132 工程特征）**。
4. 对两个目标、8 个步长分别拟合“未来负荷减当前负荷”。
   使用短历史与较长历史的正则化线性 ARX、LightGBM 和 CatBoost，
   按目标与 15–30/45–120 分钟两个时距区间学习非负、和为 1 的集成权重。
5. 树模型用逆负荷加权 MAE 对齐 MAPE，较长历史模型用 14 天半衰期降低旧工况权重。
   线性模型提供更平滑的动态外推，并非假定树模型必然优于简单模型。
6. 实验比较 14 个基础模型/参数组合，以及 4 个因果滚动偏差修正候选。
   扩大候选池的留一折误差只改善约 0.10%，未达到 0.5% 的复杂度收益阈值，
   所以最终保留较小候选池；鲁棒线性扩展与在线偏差修正**没有启用**。
7. 提交导出保留浮点往返精度，避免时钟正余弦等接近零的特征被十进制截断，
   导致保存后的输入跨过树分裂阈值。能从实际导出的 `input.csv` 重放预测。
8. 新入口遇到错误会失败，不回退到预置旧结果冒充成功。

最终启用 `ema8、ridge7、ridge21、ridge60、lgb14、lgb60、cat21`。
零权重的候选跳过最终训练。具体权重保存在 `artifacts/prelim_selected/selection.json`。

## 历史验证与局限

4 月 21、23、25 日起各两天用于开发，4 月 29 日起两天只用于最后验证，
不拟合集成权重和模型家族选择。每折训练标签必须在首个预测起点之前已到达，
例如 h=8 的最后训练特征还要额外向前剔除 2 小时。

下表三个开发窗口使用留一折权重，最后窗口使用前三折拟合的固定权重。
留一折是权重层的交叉验证，不声称这些元权重是在每个历史部署日实时获得。

| 历史窗口起点 | 单组：持续值 → 集成 MAPE | 总负荷：持续值 → 集成 MAPE |
| --- | ---: | ---: |
| 04-21 | 7.799% → 6.990% | 5.465% → 4.668% |
| 04-23 | 4.939% → 4.211% | 4.149% → 3.737% |
| 04-25 | 5.445% → 5.360% | 4.692% → 4.133% |
| 04-29，未参与权重拟合 | 5.918% → 6.190% | 6.972% → 6.386% |

最后窗口单组负荷退步，说明短历史拟合与工况变化仍然存在泛化风险。
测试集只有两天，不能凭这次改进推断其它月份或排行榜的表现。
模型参数在读取 5 月数值观测前冻结，每个起点仅使用该时刻及之前的观测。
若主办方要求整个两天闭环、不接收新观测，必须重新实现该协议，不能沿用这些分数。

## 质量扣分仍需官方规则确认

已核验：192 个起点、16 个预测字段、固定 15 分钟间隔、无缺失/重复/非有限数值，
预测在额定功率范围内，单组预测不超过总负荷。

删除的全空原始字段为 `blast_furnace_3`、`air_heater_3`、
`blast_furnace_gas_holder_1`、`converter_user3`。这解决了旧输入的明确无信息列问题，
但不能在没有评分脚本时断言 `invalid_col` 已满分。

新输入在测试两天内仍有 **8 个常数列**，来自两个真实处于零流量状态的传感器
及其工程特征。它们在历史训练中不是常数。另有 **105 个 abs(z)>3 的统计尾部单元格**。
这些诊断不等同于官方异常判定，也不一定是坏测量。
没有为了让质量检查好看而抹去合法停机、零流量，或单独修改预测后再伪造输入。

**不能保证原先少的 10 分全部追回。** 下一步最有价值的信息是官方 `out`、
`invalid_col` 的判定规则、原始提交 ZIP 或新包的完整平台反馈。
若官方按测试区间常数列扣分，应在训练内验证“停用近期无变化传感器”的独立特征方案，
而不是只删提交列、却继续让模型依赖未提交的输入。

## 复现与检查

Windows 从仓库目录运行 `./run_prelim.ps1`。
重做全部开发实验再生成结果使用 `./run_prelim.ps1 -Develop`。
Linux 使用 `bash run_prelim.sh TRAIN_DIR TEST_DIR`。

模型及文件：

- `output/prelim_v2/prelim_model.joblib`：可信本地模型，约 7.26 MiB。
- `output/prelim_v2/training_audit.csv`：逐模型、逐目标、逐步长的标签截止审计。
- `output/prelim_v2/quality_audit.json`：真实质量诊断，不是伪造的官方评分器。
- `output/prelim_v2/test_metrics.csv`：逐目标、逐步长、逐对照版本的误差与样本计数。
- `output/prelim_v2/run_metadata.json`：数据哈希、软件版本和运行耗时。

本次实测最终训练准备 268.6 秒、192 起点批量推理 0.96 秒，
包含校验、打包的总流程约 281.2 秒。该批量耗时不是在线单请求延迟承诺。
已经核验全部起点的模型序列化一致性、实际 CSV 输入重放，以及三个代表起点的
完整历史与截断历史预测一致性。

运行环境为 Python 3.13.0，依赖版本完整记录于元数据。
官方建议的 Python 3.10 尚未在本机单独验证；跨版本请重新训练，
不要将 scikit-learn/joblib 模型的跨版本二进制加载视为保证。

新提交 ZIP 的 SHA256：

`8989f132bee7fcdc64c98cfe13527f743241704b27c2c693b3399a56667d2935`
